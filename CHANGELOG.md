# 九九小课堂 - 开发日志

## v1.9.1 (2026-04-18)

### 练一练模式二选一交互重构
- 准备页顶部新增「🎲 自由练习」/「🎯 专项练习」模式切换按钮（卡片式大按钮）
- 两种模式**互斥**，选中哪个只显示对应的配置区域，减少页面混乱：
  - 自由练习：显示「选择练习范围」
  - 专项练习：显示「选择专项题库」（薄弱项 / 犹豫慢题勾选）
- 「选择题数」是两种模式共用，放在下方
- 切换到自由模式时自动清空专项勾选
- 校验逻辑分模式处理：
  - 自由模式：未选范围时提示"请先选择练习范围"
  - 专项模式：未勾选时提示"请勾选要练习的专项题库"
- 成绩单专练入口（薄弱项/犹豫题/摸底重点题）自动切换到专项模式

## v1.9.0 (2026-04-18)

### 练一练支持精准专项练习
- 准备页新增「🎯 专项练习（可选）」勾选区域，可勾选：
  - **⚠️ 仅练薄弱项**：只出错误率高的具体题目
  - **🐢 仅练犹豫慢题**：只出平均答题时间 ≥ 5 秒的具体题目
  - 两者可同时勾选，题库自动合并去重
- 勾选后实时显示每类题库数量（如 `(5)`），数量为 0 时按钮自动置灰禁用
- 专项模式下忽略上方「练习范围」，直接从精准题库中随机出题
- 新增出题防连续重复逻辑，最多重试 5 次避免同一题连续出现
- 练习结束回到准备页时自动刷新题库数量（答题后薄弱项会动态变化）
- 切换到「练一练」tab 时也会刷新数量

### 成绩单专练入口改为精准命中
- **💪 专项练习薄弱项**：原先是把薄弱项数字扩到整行乘法范围（比如错了一个 3×7，会练整个 3 的乘法和 7 的乘法），现改为只练薄弱项的具体算式
- **⚡ 专练犹豫题**：同样改为只练犹豫的具体算式
- **💪 专练摸底重点题**：只练上次摸底中答错或犹豫的具体题目
- 这些入口都直接复用 Tab 上的专项勾选机制，UI 与逻辑统一

## v1.8.3 (2026-04-12)

### 数字键盘点击视觉效果增强
- 按下缩放从 0.88 增强到 0.82，体感更明显
- 按下背景色变化更醒目（数字键 #BFBFBF、删除键 #FF9E9E）
- 新增 `::after` 伪元素涟漪扩散效果（ripple），从按钮中心水波纹式扩散
- 光波（tap-flash）扩散范围从 12px 增大到 24px，亮度更高
- 不同按钮类型独立光波颜色：数字键紫色、确认键橙色、删除键红色
- 使用 `cubic-bezier(.4,0,.2,1)` 缓动函数，过渡更流畅有弹性

## v1.8.2 (2026-04-12)

### 练一练模块优化
- 「结束练习」按钮从顶部标题栏移至跳过按钮右侧，操作更顺手
- 新增题数选择功能：开始练习前可选择本轮练习题数
  - 提供 6 个档位：10题、20题、30题、50题、100题、不限
  - 默认选择 20 题
  - 答题区顶部实时显示进度（如 "5/20"）
  - 达到目标题数后自动结束练习，显示完成提示和正确率
  - 跳过的题目也计入题数
  - 从成绩单/摸底跳转的专练同样支持题数限制

### 首次访问自动检测屏幕比例
- 首次打开页面时自动检测屏幕宽高比，匹配最接近的比例模式（16:9、16:10、3:2、4:3）并自动应用
- 自动检测后 Toast 提示用户当前检测到的比例模式
- 通过 localStorage 标记首次访问状态，后续访问不再重复检测
- 用户手动切换比例后，手动选择优先级高于自动检测

### 禁止页面缩放
- viewport meta 标签增加 `maximum-scale=1.0, user-scalable=no`，从浏览器层面禁止缩放
- CSS 添加 `touch-action: manipulation`，阻止双击缩放的浏览器默认行为
- JS 监听 `gesturestart/gesturechange/gestureend` 事件，阻止 Safari 双指缩放手势
- JS 监听 `touchstart/touchmove` 事件，阻止多指触摸缩放（兼容非 Safari 浏览器）
- JS 监听 `touchend` 事件并计算双击间隔（<300ms），阻止快速双击放大
- JS 监听 `wheel` 事件，阻止 Ctrl+滚轮缩放（桌面端防护）

## v1.8.1 (2026-04-12)

### 项目更名
- 项目标题从「99乘法表学习小工具」更名为「九九小课堂」
- 统一更新 HTML title、帮助页版本信息、manifest.json、README.md、CHANGELOG.md 中的名称
- 移除标题中的 emoji 符号

### UI 优化
- 语音开关按钮移至页面右上角，全局可见且不遮挡内容
- 学一学页面的「自动朗读」按钮从标题栏右上角移至九九表框内顶部
- 练习范围选中按钮增加绿色对勾角标，选中状态更醒目
- 练习范围默认全部未选，需手动选择后才能开始练习
- 「全部」按钮支持切换选中/取消全选
- 未选择范围时点击开始练习会提示「请先选择练习范围」
- 学一学页面九九表行间距加大，阅读更舒适
- 左上角「九九小课堂」下方增加 by FeimanZ 作者落款
- 犹豫阈值从 8 秒调整为 5 秒，判定更灵敏
- 右上角新增刷新按钮，点击可快速刷新页面
- 帮助页底部版本信息下方增加 by FeimanZ 作者落款
- 右上角新增推荐按钮，点击可复制推荐文案和链接到剪贴板，方便分享给朋友
- 右上角推荐/刷新/语音三个按钮间距加大，避免在小屏幕上互相遮挡
- 语音开关按钮去掉文字和状态标签，改为纯图标圆形按钮，与刷新/推荐按钮风格统一，彻底消除重叠问题

### 屏幕比例手动切换
- 帮助中心新增「📐 屏幕适配」卡片，支持手动切换5种屏幕比例模式
  - **自动**：使用默认 @media 响应式适配（推荐）
  - **16:9**：宽屏手机/笔记本模式
  - **16:10**：华为平板/MacBook 模式
  - **3:2**：Surface/部分平板模式
  - **4:3**：iPad/方屏平板模式
- 通过给 body 添加对应的 CSS class（如 `ratio-16-9`）强制应用该比例下的布局样式
- 实时显示当前屏幕分辨率、DPR、宽高比及自动识别的最接近比例
- 选择后立即生效，设置通过 localStorage 自动保存，下次打开自动恢复

### 平板横屏适配
- 新增针对平板设备（华为平板 2560×1600 16:10、iPad 4:3 等）的响应式适配
- 新增三组平板专用媒体查询断点：
  - `max-width:1400px + min-height:600px`：覆盖华为平板(dpr=2, 1280×800)、三星平板等
  - `max-width:1100px + min-height:600px`：覆盖 iPad(1024×768)、小尺寸平板、高DPI平板
  - `max-aspect-ratio:16/10 + min-height:600px`：精确匹配非宽屏比例设备（4:3等）
- 平板断点全面优化：侧栏宽度、内容区间距、乘法表格子大小、数字键盘尺寸、答题卡片布局
- 适配主流横屏比例：16:9、16:10、3:2、4:3

## v1.8.0 (2026-04-12)

### 🔄 练习/闯关数据纳入成绩单 & 统一犹豫阈值

#### 功能
- **练习模式答题记录纳入成绩单**：每道练习题的详细记录（答案、耗时、是否犹豫）保存到 `practiceHistory`，成绩单中的薄弱项和犹豫题会包含练习数据
- **闯关模式答题记录纳入成绩单**：每次闯关的详细记录（每题答案、耗时、是否犹豫）保存到 `challengeHistory`，成绩单统计包含闯关数据
- **统一犹豫阈值标准**：新增全局常量 `HESITANT_THRESHOLD_SEC = 8`，练习、闯关、摸底三个模式统一使用 ≥8秒 判定为犹豫
- **成绩单新增来源分类统计**：展示练习答题数、闯关答题数、摸底答题数，让数据来源一目了然
- **成绩单打开时全量重建统计**：`updateReport()` 开始时调用 `rebuildQuestionStatsFromHistory()` 汇总三个来源的数据

#### 数据结构
- `state.practiceHistory`：练习历史记录数组，最多保留500条，每条含 `{key, a, b, answer, correct, isCorrect, timeTaken, isHesitant, date, source}`
- `state.challengeHistory`：闯关历史记录数组，最多保留20次，每次含 `{date, difficulty, records, totalTime, correctCount, totalCount}`
- `challengeState.records`：当次闯关的每题记录数组
- `HESITANT_THRESHOLD_SEC`：全局犹豫阈值常量（8秒）

#### 改进
- `rebuildQuestionStatsFromHistory()` 扩展为汇总练习 + 闯关 + 摸底三个来源
- `checkPractice()` 新增犹豫判断和详细记录保存
- `checkChallenge()` 新增犹豫判断和详细记录保存
- `finishChallenge()` 新增保存闯关历史到 `state.challengeHistory`
- `loadState()` 新增数据迁移：确保 `practiceHistory`/`challengeHistory` 字段初始化
- 犹豫题卡片标题更新为「答题≥8秒」，空状态提示包含所有模式
- 日志输出增加犹豫标记信息

## v1.7.0 (2026-04-12)

### ✨ 新增帮助页面 & 移除首页弹窗

#### 功能
- **移除首次进入引导弹窗**：不再在首次进入时弹出引导弹窗，页面直接展示主内容
- **新增帮助页面（❓ 帮助）**：左侧导航栏新增帮助 Tab
  - **左栏：添加到主屏幕指引**
    - 分浏览器（Safari / Chrome / 华为浏览器）提供详细图文步骤
    - 自动检测用户浏览器类型，默认切换到对应标签
    - 含 SVG 示意图，直观指引操作步骤
  - **右栏：功能介绍 & 使用技巧**
    - 学一学、练一练、闯关赛、摸底测试、成绩单五大功能说明
    - 横屏体验、语音朗读、数字键盘、数据保存等使用技巧
    - 底部显示版本号
- 左侧导航栏新增第6个按钮「❓ 帮助」
- `tabMap` 新增 `help` 映射，支持帮助页面切换
- 切换到帮助 Tab 时自动检测浏览器类型并高亮对应标签

#### 清理
- 移除 `welcomeOverlay` HTML 弹窗（含横屏提示和添加到主屏幕引导内容）
- 移除弹窗相关 CSS（`.welcome-overlay`、`.welcome-dialog`、`.welcome-header` 等）
- 移除弹窗相关 JS 函数（`showWelcomeIfNeeded()`、`closeWelcome()`）
- 移除 `WELCOME_KEY` localStorage 标记
- 保留 `switchA2HSTab()` 和 `detectAndSwitchBrowserTab()` 供帮助页面使用

## v1.6.0 (2026-04-12)

### ✨ 自动横屏显示

#### 功能
- **自动横屏显示（多重方案）**：
  - **PWA Manifest**：新增 `manifest.json`，声明 `orientation: landscape`，从主屏幕图标启动时自动横屏
  - **Screen Orientation API**：启动时自动调用 `screen.orientation.lock('landscape')`，兼容旧版 API
  - **竖屏提示遮罩**：检测到竖屏时显示全屏引导遮罩（渐变背景+旋转手机动画+示意图），提示用户旋转设备
  - 用户可点击「暂时竖屏使用」跳过提示；旋转回横屏后自动隐藏
  - 桌面端（横屏）不显示任何提示
  - 新增 `apple-mobile-web-app-capable`、`mobile-web-app-capable` meta 标签支持 PWA 全屏体验

## v1.5.3 (2026-04-12)

### 🔊 修复：华为平板浏览器语音不出声

#### 问题
- 在华为平板自带浏览器中运行时，语音朗读算式无声音
- 根因：华为/鸿蒙自带浏览器的 `speechSynthesis` API 虽存在但功能不完整，`speak()` 调用后既不触发 `onend` 也不触发 `onerror`，导致静默失败

#### 修复
- **新增语音引擎初始化检测** (`initSpeechEngine`)：
  - 启动时自动枚举所有可用语音，优先选择本地中文语音
  - 进行静音语音测试，检测引擎是否真正可用
  - 1.5秒超时兜底：若 speak 无任何回调，判定语音不可用
- **智能选择最佳中文语音** (`bestChineseVoice`)：
  - 优先级：本地zh-CN > zh-CN > 本地中文 > 任意中文
  - 所有 `speak()` 和自动朗读调用均使用选定的最佳语音
- **自动朗读超时兜底**：
  - `readNextFormula` 添加5秒超时检测，避免语音卡死导致朗读停止
  - 超时后自动取消并跳到下一条口诀
- **语音开关增强诊断**：
  - 开启语音时进行实时可用性测试，2秒超时检测
  - 若检测到语音不可用，弹出提示建议用户换用 Chrome
- **启动时诊断日志**：
  - 延迟3.5秒输出语音引擎完整诊断信息（API支持、语音数量、最佳语音、UA等）
  - 检测到不可用时自动弹出 Toast 提示

#### 建议
- 华为平板建议安装 Chrome 或 Edge 浏览器以获得完整语音体验

## v1.5.2 (2026-04-12)

### 🐛 修复：清除数据后页面未刷新

#### 问题
- 点击"清除所有学习数据"后，成绩单页面的数据（正确率、答题数、薄弱项、摸底概况等）仍显示旧值，需要手动切换页面才会更新

#### 修复
- `clearAllData()` 中新增调用 `updateReport()` 刷新成绩单所有数据
- 新增调用 `checkAssessHistory()` 重置摸底历史入口
- 新增调用 `resetAssessView()` 重置摸底页面状态

## v1.5.1 (2026-04-12)

### ✅ 答题输入框移到等号右侧

#### 🎯 布局调整
- 将三个模式（练习、挑战、摸底）的答题输入框从公式下方独立一行，移到**等号右侧**与算式同行显示
- 效果：`3 × 5 = [?]`（输入框紧跟等号）
- `.quiz-question` 改为 `display:flex` 行内布局，居中对齐
- 移除不再需要的 `.quiz-input-wrap` 容器

#### 📱 响应式适配
- 各屏幕断点新增 `.quiz-question .quiz-input` 尺寸规则
- 中等屏幕：输入框 100×50px，font 34px
- 小屏幕：输入框 80×40px，font 26px
- 超小屏幕：输入框 66×34px，font 22px

## v1.5.0 (2026-04-12)

### ✅ 移除答题区"确认"按钮 & 数字键盘体验优化

#### 🗑️ 移除页面"确认"按钮
- 移除练习模式、挑战模式、摸底模式答题区的"✅ 确认"按钮
- 练习模式保留"⏭️ 跳过"按钮
- 确认操作统一通过右侧数字键盘的 OK 按钮完成

#### 🎨 数字键盘确认按钮样式升级
- 确认按钮文字从 ✓ 改为 **OK**，更直观醒目
- 确认按钮配色从紫绿渐变改为 **橙色暖色系渐变**（#FF6B35 → #FF8C42 → #FFB347）
- 添加投影效果和文字阴影，与数字按钮形成明显区分
- hover 时放大 + 加深投影

#### 📳 点击反馈增强
- 所有按钮 active 状态：缩放从 0.95 → **0.88**，更明显的按压感
- 新增 `tap-flash` 涟漪光晕动画（紫色扩散光圈）
- 新增 **振动反馈**（navigator.vibrate API）：
  - 数字按钮：轻触振动 10ms
  - 删除按钮：中等振动 20ms
  - OK 确认按钮：节奏振动 [15ms, 30ms, 15ms]
- hover 时微放大 1.03 倍，提供视觉预期
- 删除按钮 active 状态独立颜色 #FFBABA

## v1.4.5 (2026-04-12)

### 🐛 修复：右侧键盘区宽度在浏览器中未生效

#### 问题原因
- 基础样式设置了 `width:400px`，但响应式媒体查询全部基于 `max-height`（屏幕高度）
- 大多数浏览器窗口高度 < 600px（尤其笔记本），导致 `@media (max-height:600px)` 触发，将宽度覆盖为极小值
- 之前的响应式断点中键盘区宽度过小（180px/190px），与基础样式 400px 差距过大

#### 修复内容
- **中等屏幕** (max-height:600px)：右侧宽度 180px → **280px**，按钮 font 20px → **28px**，min-height **56px**，gap **10px**
- **小屏幕** (max-height:450px)：右侧宽度 190px → **240px**，按钮 font 22px → **24px**，min-height **46px**，gap **7px**
- **超小屏幕** (max-height:360px)：保持 180px，按钮 padding 调整为 10px
- **新增基于宽度的媒体查询**：
  - `@media (max-width:900px)`：右侧 280px，按钮 font 28px / min-height 56px
  - `@media (max-width:700px)`：右侧 220px，按钮 font 22px / min-height 42px

## v1.4.4 (2026-04-12)

### ✅ 右侧键盘区再次加宽

#### 🔢 右侧区域 & 数字按钮加大（改进）
- 三个模式右侧区域宽度统一增大到 **400px**
- 数字按钮 padding **28px**，字号 **40px**，最小高度 **72px**，圆角 18px
- 按钮间距 gap **16px**，容器内边距 **20px**

#### 📱 响应式适配
- 中等屏幕：右侧 280px，按钮 font 28px / min-height 56px / gap 10px
- 小屏幕：右侧 240px，按钮 font 24px / min-height 46px / gap 7px
- 超小屏幕：右侧 180px，按钮 font 18px / min-height 34px

## v1.4.3 (2026-04-12)

### ✅ 退出弹窗闪烁修复 & 文案精简

#### 🐛 弹窗闪烁修复（Bug Fix）
- 弹窗之前使用了 `feedbackPop` 动画（答题反馈专用），该动画包含 `translate(-50%,-50%)` 偏移和最终 `opacity:0` 淡出，导致弹窗先在左上角闪现再回到中间
- 新增专用 `dialogPop` 动画：从 `scale(.8)` + `opacity:0` → `scale(1)` + `opacity:1`，直接在屏幕中心弹出，无偏移无闪烁

#### ✍️ 文案精简（改进）
- 标题：`确认退出${label}？` → **`确认退出？`**
- 描述：`当前${label}进度将不会保存，\n确定要离开吗？` → **`当前进度不会保存`**
- 继续按钮：`继续${label}` → **`继续`**（避免文字过长换行）
- 所有按钮添加 `white-space:nowrap` 防止换行

## v1.4.2 (2026-04-12)

### ✅ 数字键盘区再次增大加宽

#### 🔢 右侧区域加宽（改进）
- 练习/闯关赛/摸底测试右侧区域宽度从 280px 增大到 **360px**（+80px）
- 数字键盘容器内边距统一增大到 **20px**

#### 🔢 数字按钮增大（改进）
- 按钮 padding 从 22px 增大到 **26px**
- 按钮字号从 34px 增大到 **38px**
- 按钮最小高度从 60px 增大到 **68px**
- 按钮圆角从 16px 增到 **18px**
- 按钮间距 gap 从 12px 增大到 **14px**

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：右侧 260px，按钮 padding 16px / font 28px / min-height 54px / gap 10px
- 小屏幕（≤450px高）：右侧 220px，按钮 padding 12px / font 24px / min-height 44px / gap 7px
- 超小屏幕（≤360px高）：右侧 170px，按钮 padding 8px / font 18px / min-height 32px

## v1.4.1 (2026-04-12)

### ✅ 答题禁止键盘输入，仅支持数字键盘点击

#### 🚫 禁止键盘输入（改进）
- 三个答题输入框（练习/闯关赛/摸底测试）全部改为 `readonly` + `inputmode="none"`
- `type` 从 `number` 改为 `text`，避免移动端弹出数字键盘spinner
- 移除 `onkeypress` 回车提交事件，所有提交只能通过右侧数字键盘的 ✓ 按钮

#### 🔒 多重防护机制（新增）
- CSS：`caret-color:transparent` 隐藏光标，`user-select:none` 禁止选中
- JS：拦截 `keydown`/`keypress`/`keyup` 阻止所有键盘按键
- JS：拦截 `paste` 和 `drop` 阻止粘贴和拖拽输入
- JS：`focus` 事件立即 `blur()`，防止移动端虚拟键盘弹出
- 初始化时自动执行 `disableKeyboardInput()` 完成防护注册

## v1.4.0 (2026-04-12)

### ✅ 左上角App名称品牌化，与Tab按钮区分

#### 🎨 App名称样式重设计（改进）
- 🧮 emoji 独立为 `.app-logo`，字号增大到 **32px**，带阴影投影效果
- "乘法小工具" 文字独立为 `.app-name`，包裹在半透明白色圆角背景中（`rgba(255,255,255,.2)`）
- 取消旧的 `border-bottom` 分隔线，改用视觉层次区分
- 增加 `letter-spacing:1px` 和 `text-shadow` 提升品牌质感
- 整体 padding 加大（`8px 6px 10px`），与下方 Tab 按钮拉开间距

#### 📱 响应式适配（改进）
- 所有四档响应式断点（500px/420px高度、900px/700px宽度）均同步适配新结构
- 小屏幕下 logo emoji 和文字同比例缩小但保持品牌辨识度

## v1.3.9 (2026-04-12)

### ✅ 学一学乘法表字体再次增大

#### 📖 乘法表格子增大（改进）
- 默认字号从 20px 增大到 **26px**，padding 从 `10px 14px` 增大到 `12px 16px`
- 格子最小宽度调整为 108px，圆角从 12px 增到 14px
- 行间距 gap 从 10px 增大到 **12px**
- 朗读高亮状态字号从 24px 增大到 **30px**，缩放比从1.25降到1.2避免过大

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：字号 13px → **20px**，padding `8px 12px`，min-width 90px
- 小屏幕（≤450px高）：字号 11px → **16px**，padding `6px 8px`，min-width 74px
- 超小屏幕（≤360px高）：字号 12px → **14px**，padding `4px 6px`，min-width 62px
- 7、8、9行格子较多时自动换行（`flex-wrap:wrap`），充分利用上下空间

## v1.3.8 (2026-04-12)

### ✅ 数字输入区进一步增大加宽

#### 🔢 数字键盘再次增大（改进）
- 右侧区域宽度从 280px 增大到 **340px**（+60px）
- 数字按钮 padding 从 18px 增大到 **22px**，字号从 30px 增大到 **34px**
- 按钮最小高度从 52px 增大到 **60px**，圆角从 14px 增到 16px
- 按钮间距 gap 从 10px 增大到 **12px**
- 数字键盘容器内边距统一增大到 **18px**

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：右侧 240px，按钮 padding 14px / font 26px / min-height 50px / gap 8px
- 小屏幕（≤450px高）：右侧 190px，按钮 padding 10px / font 22px / min-height 40px / gap 5px
- 超小屏幕（≤360px高）：右侧 160px，按钮 padding 8px / font 18px / min-height 32px

## v1.3.7 (2026-04-12)

### ✅ 学一学乘法表文字增大 & 长行自动换行

#### 📖 乘法表格子增大（改进）
- 默认字号从 16px 增大到 **20px**，padding 从 `8px 10px` 增大到 `10px 14px`
- 格子最小宽度从 96px 调整为 100px，圆角从 10px 增到 12px
- 行间距 gap 从 8px 增大到 **10px**，更通透易读
- 朗读高亮状态字号从 20px 增到 **24px**

#### 🔄 长行自动换行（新增）
- `.table-row` 的 `flex-wrap` 从 `nowrap` 改为 **`wrap`**
- 最后几行（如7×1~7×7、8×1~8×8、9×1~9×9）数字较多时，格子自动换行到下一排
- 充分利用屏幕上下空间，不再在水平方向挤压

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：字号 13px → **16px**，最小宽度 74px → 84px
- 小屏幕（≤450px高）：字号 11px → **14px**，最小宽度 60px → 70px
- 超小屏幕（≤360px高）：字号 10px → **12px**，最小宽度 52px → 58px

## v1.3.6 (2026-04-12)

### ✅ 答题正确/错误提示音和视觉增强

#### 🔊 提示音效（新增）
- 使用 Web Audio API 生成提示音，无需加载外部音频文件
- **答对**：欢快的升调两连音（C5→E5），清脆明亮
- **答错**：低沉短促的嗡鸣（200Hz→150Hz），提醒但不刺耳
- 提示音独立于语音开关，关闭语音仍有音效反馈
- 音效在 AudioContext 不可用时优雅降级，不影响功能

#### 🎨 视觉反馈增强（新增）
- **答对**：整张答题卡片闪绿光（`flashCorrect` 动画），box-shadow 扩散绿色光晕
- **答错**：整张答题卡片左右抖动 + 闪红光（`flashWrong` 动画），6步往复抖动
- 动画持续约 0.5~0.6 秒，自动清除 class 以支持重复触发
- 三个答题模式（练习/闯关/摸底）均已集成

#### 🔧 技术改进
- 新增 `playCorrectSound()` / `playWrongSound()` —— Web Audio API 合成音效
- 新增 `flashCard(cardElement, isCorrect)` —— 统一卡片闪烁动画
- 新增 CSS 动画 `@keyframes flashCorrect` / `@keyframes flashWrong`
- 三个答题卡片分别添加 id：`practiceCard`（已有）、`challengeCard`、`assessCard`
- `checkPractice()` / `checkChallenge()` / `checkAssessment()` 中集成音效和闪烁

## v1.3.5 (2026-04-12)

### ✅ 答题数字输入区增大，提升点击体验

#### 🔢 数字键盘优化（改进）
- 右侧数字输入区宽度从 240px 增大到 **280px**（各响应式档位同步增大40px）
- 数字按钮 padding 从 14px 增大到 **18px**，字号从 26px 增大到 **30px**
- 按钮间距 gap 从 7px 增大到 **10px**，圆角从 12px 增到 14px
- 新增 `min-height` 保证按钮最小高度（默认52px），按钮内容居中对齐
- 数字键盘容器内边距从 12px 增到 14px

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：右侧 220px，按钮 padding 12px / font 24px / min-height 44px
- 小屏幕（≤450px高）：右侧 180px，按钮 padding 8px / font 20px / min-height 36px
- 超小屏幕（≤360px高）：右侧 140px，按钮 padding 6px / font 16px / min-height 28px

## v1.3.4 (2026-04-12)

### ✅ 语音开关增加常驻文字状态标识

#### 🔊 语音开关优化（改进）
- 右下角语音开关增加常驻文字状态标识，拆分为「语音」文字 + 「已开/已关」状态标签
- 新增 `.voice-status` 状态标签：开启时紫色背景，关闭时红色背景，视觉对比更强
- 文字标签在所有屏幕尺寸下**常驻显示**，不再在小屏幕隐藏
- 关闭状态文字颜色改为红色（#FF6B6B），开启状态保持主色调紫色

#### 📱 响应式适配（改进）
- 中等屏幕（≤600px高）：状态标签缩小至10px
- 小屏幕（≤450px高）：文字10px，状态标签9px，保持可见
- 超小屏幕（≤360px高）：进一步缩小但依然常驻显示

## v1.3.3 (2026-04-12)

### ✅ 所有页面统一顶部大标题布局

#### 🎨 统一页面标题（改进）
- 所有页面新增统一的 `.page-top-header` 顶部大标题栏
- 大标题栏采用渐变半透明背景，居中展示页面名称和副标题
- **学一学**：标题「📖 九九乘法表」+ 副标题 + 右侧自动朗读按钮
- **练一练**：设置页和答题页都有标题「✏️ 练一练」，答题页右侧有结束按钮
- **闯关赛**：设置页「🏆 限时闯关」、答题页「🏆 限时闯关」、结果页「🏆 闯关结果」
- **摸底测试**：介绍页「🔍 摸底测试」、答题页「🔍 摸底测试」、结果页「🔍 摸底结果」
- **成绩单**：标题「📊 学习成绩单」+ 副标题
- 摸底结果页内部的大标题改为 `<h3>` 避免与顶部重复

#### 📱 响应式适配（改进）
- `.page-top-header` 四档高度适配：正常/600px/450px/360px
- 小屏幕自动缩小标题字号、内边距和间距
- 超小屏幕隐藏副标题节省空间

#### 🔧 技术改进
- 新增 `.page-top-header` / `.page-subtitle` / `.header-actions` CSS 类
- 所有子页面切换从 `display:block` 统一为 `display:flex`，配合 flex-direction:column 布局
- `challengePlay`/`challengeResult`/`assessPlay`/`assessResult`/`practicePlay` 显示逻辑统一改为 flex
- 移除练习答题区内部的冗余结束按钮，改在顶部标题栏操作区展示

## v1.3.2 (2026-04-12)

### ✅ 成绩单摸底概况 - 多次摸底数据综合展示

#### 📊 摸底概况板块（新增）
- 成绩单新增**「🔍 摸底概况」**卡片，位于总体正确率下方
- 展示摸底统计数据：**摸底次数**、**最近正确率**、**最佳正确率**、**平均用时**
- 多次摸底后显示**较上次趋势**：↑提升（绿色）、↓下降（红色）、→持平（橙色）
- **正确率趋势柱状图**：至少2次摸底后显示，最多展示最近10次
  - 柱形颜色动态变化：≥90%绿色、≥70%橙色、<70%红色
- **历次摸底记录列表**：倒序展示所有摸底历史，点击可跳转查看该次详细结果

#### 🔄 多次摸底数据综合刷新（改进）
- 新增 `assessmentHistory` 数组保存所有摸底历史（最多保留20次）
- 每次摸底完成后，通过 `rebuildQuestionStatsFromHistory()` **基于全部历史记录重建 questionStats**
- 薄弱项和犹豫慢题根据多次摸底的累计数据综合判断，而非只看单次
- 旧数据自动迁移：已有 `lastAssessment` 的用户数据自动补充到 `assessmentHistory`

#### 🔧 技术改进
- 新增 `assessmentHistory` 状态字段，存储所有摸底结果数组
- 新增 `rebuildQuestionStatsFromHistory()` 函数，基于全部摸底历史重建全局题目统计
- 新增 `renderAssessSummary()` 函数，渲染成绩单中的摸底概况板块
- 新增 `formatTimeShort()` 时间格式化辅助函数
- 新增 `viewHistoryAssess(index)` 点击历史记录跳转查看详情
- `loadState()` 增加数据迁移逻辑，兼容旧版 `lastAssessment` 数据
- `finishAssessment()` 改为追加历史+统一重建 `questionStats`
- `checkAssessment()` 不再在答题时即时累加全局统计（改为完成时统一重建）
- `checkAssessHistory()` 支持显示历史次数
- `showAssessHistory()` 优先从 `assessmentHistory` 取最新记录
- 摸底概况板块全面适配 max-height:600px / 450px 两档响应式缩放

## v1.3.1 (2026-04-12)

### ✅ 学一学乘法表改为梯形展示

#### 📖 乘法表展示优化（改进）
- 乘法表改为**经典梯形样式**：第1行1个格子，第2行2个格子...第9行9个格子
- 每个格子**固定宽度**（96px），不再拉伸填满整行
- 行**左对齐**，自然形成梯形阶梯效果，符合传统乘法表展示习惯
- **增加间距**：行间距和列间距从4px增加到8px，整体更松散易读
- **增大内边距**：格子padding从6px 8px增加到8px 10px
- **增大字号**：格子字号从15px增加到16px
- **增大圆角**：格子圆角从8px增加到10px
- 表格整体padding从14px增加到18px

#### 🔊 自动朗读口诀（新增）
- 学一学页面新增**标题栏**和**自动朗读按钮**
- 点击「🔊 自动朗读」按钮，从1×1开始按顺序逐个朗读所有口诀
- 朗读中按钮变为红色「⏸️ 暂停朗读」，支持随时暂停
- 暂停后按钮变为橙色「▶️ 继续朗读」，从上次位置继续
- **当前朗读的口诀会放大高亮**：1.25倍缩放 + 紫色边框 + 阴影效果 + 字号增大
- 朗读完一个口诀后自动滚动到下一个，确保当前朗读口诀始终可见
- 全部45个口诀朗读完毕后自动停止，弹出完成提示
- 切换Tab时自动停止朗读

#### 📱 全屏幕响应式适配（改进）
- **侧边导航栏**：小屏幕下自动缩窄（110px→80px→64px→56px），图标和文字等比缩小
- 侧边栏增加 `overflow-y:auto` 确保所有按钮可滚动访问
- **四档高度适配**（max-height: 600px/500px/450px/360px）：
  - 题目字号：60px→42px→32px→26px
  - 输入框、按钮、统计项等全面等比缩小
  - 数字键盘区域宽度：240px→180px→140px→110px
  - 乘法表格子：96px→74px→60px→52px
- **宽度适配**（max-width: 900px/700px）：侧边栏进一步缩窄
- 各模块（挑战卡片、摸底卡片、热力图、成绩单等）全部跟随缩放
- 语音开关在极小屏幕隐藏文字只显示图标
- 反馈动画大小也随屏幕缩放

#### 🚪 切换Tab退出确认弹窗（新增）
- 在**练习/挑战/摸底**进行中切换Tab时，弹出自定义确认弹窗
- 弹窗显示当前正在进行的模式名称（练习/闯关挑战/摸底测试）
- 点击「继续练习」留在当前模式，点击「退出」清理状态并切换
- 点击遮罩层也可取消退出
- **挑战模式退出**：自动清除计时器，重置到挑战设置页
- **摸底测试退出**：自动清除题目计时器，重置到摸底介绍页
- **练习模式退出**：重置到练习准备页
- 通过内部跳转（如成绩单→专练薄弱项）不触发弹窗（因此时不在答题中）

#### ✏️ 练一练增加准备页（改进）
- 首次进入练一练时，显示**准备页面**而非直接开始答题
- 准备页包含标题、说明、范围选择按钮和「🚀 开始练习！」按钮
- 点击「开始练习」后才进入答题界面，自动出题和朗读
- 答题页增加「⏹️ 结束练习」按钮，可随时返回准备页
- 从成绩单/摸底结果跳转"专练"时，直接进入答题不经过准备页
- 页面初始化不再预生成练习题，不再触发语音朗读

#### 🔊 语音修复
- 修复页面刷新时会自动朗读出题声音的问题

#### 🔧 技术改进
- 新增 `currentTab` 全局变量，跟踪当前所在Tab
- 新增 `practiceStarted` 状态标记练习是否开始
- 新增 `startPractice()` / `stopPractice()` 控制练习生命周期
- 新增 `getActiveSession()` 检测当前是否有进行中的答题
- 新增 `showExitConfirm(session, targetTab)` 自定义弹窗组件
- 新增 `cleanupSession(type)` 清理进行中状态（清除定时器、重置UI）
- 新增 `autoReadState` 对象管理自动朗读状态（isReading/isPaused/currentRow/currentCol/timer）
- 新增 `toggleAutoRead()` / `startAutoRead()` / `pauseAutoRead()` / `resumeAutoRead()` / `stopAutoRead()` 朗读控制函数
- 新增 `readNextFormula()` 核心朗读循环，基于 `SpeechSynthesisUtterance.onend` 事件驱动
- 新增 `advanceAutoRead()` 口诀遍历逻辑（j: 1→i, i: 1→9）
- 新增 `highlightAutoReadCell(j, i)` 朗读时高亮并滚动到当前口诀
- 新增 `updateAutoReadBtn()` 按钮状态切换（朗读中/暂停/默认）
- `doSwitchTab()` 增加离开learn时自动停止朗读的逻辑
- `switchTab()` 拆分为检测+确认+执行三步，核心切换逻辑移入 `doSwitchTab()`
- `newPracticeQuestion()` 新增 `silent` 参数，初始化时静默生成题目不触发语音
- `renderRangeButtons()` 支持同时渲染准备页和答题页两个容器
- `init()` 不再预生成练习题和创建练习键盘

---

## v1.3.0 (2026-04-12)

### ✅ 横屏16:9全屏布局改造 + 摸底双模式

#### 🖥️ 横屏16:9布局（重大改造）
- 整体改为**左侧导航栏 + 右侧内容区**的横屏布局
- 页面在16:9显示器上**一屏完整展示**，无需滚动
- **左侧导航**：渐变色彩竖排导航，图标+文字，大按钮易操作
- 所有模块字号全面加大：题目 60px、输入框 42px、按钮 18px+，适合小朋友操作
- **练习/挑战/摸底**答题页面：左侧答题区 + 右侧数字键盘并排布局
- **摸底结果**页面：左侧热力图 + 右侧关注列表/答题记录双栏展示
- **成绩单**页面：左侧正确率/数据 + 右侧薄弱项/犹豫题双栏展示
- 移除旧的顶部header和水平tab导航

#### 🔍 摸底双模式（新增）
- 摸底介绍页增加**模式选择卡片**：
  - 📝 **顺序摸底**：从 1×1 到 9×9 按顺序逐题作答
  - 🎲 **乱序摸底**：45道题 Fisher-Yates 洗牌随机出题
- 答题页面显示当前模式标签（📝 顺序 / 🎲 乱序）
- 摸底结果页显示本次测试的模式信息
- 模式选择自动保存到 localStorage，下次默认上次选择

#### 🔧 技术改进
- `defaultState` 新增 `assessMode: 'order'` 字段
- `generateAssessQuestions(mode)` 接受 mode 参数，条件执行洗牌
- `assessState` 新增 `mode` 字段记录当前测试模式
- `assessmentResult` 保存 `mode` 信息
- Tab 切换从 `.tab-btn` 改为 `.nav-btn`（匹配新导航结构）
- CSS 全面重写为横屏优先布局

---

## v1.2.0 (2026-04-12)

### ✅ 出题语音朗读 + 语音开关

#### 🔊 出题语音朗读（新增）
- **练习模式**：每次出新题时自动语音朗读"X乘Y等于几？"
- **挑战模式**：每道题出现时自动语音朗读算式
- **摸底测试**：每道题出现时自动语音朗读算式
- 答对后随机播放鼓励语音（"答对了，真棒！""太厉害了！""完全正确！"等5种）
- 答错后语音朗读正确答案并鼓励记忆
- 挑战结束后根据成绩给予不同语音总结评价
- 跳过练习题时也会语音朗读正确答案

#### 🔇 语音开关（新增）
- 右下角悬浮语音开关按钮，可一键开/关语音
- 开启时显示 🔊 + 脉冲动画，关闭时显示 🔇
- 语音状态自动保存到 localStorage，下次打开保持上次设置
- 移动端自动隐藏文字标签，只保留图标

#### 🔧 技术改进
- `speak()` 函数升级：增加开关控制、自动取消排队语音、优化语速和音调
- 新增 `speakQuestion(a, b)` 出题专用语音函数
- 新增 `speakResult(isCorrect, a, b, correct)` 答题反馈语音函数
- 新增 `toggleVoice()` / `updateVoiceToggleUI()` 语音开关相关函数
- `defaultState` 新增 `voiceEnabled: true` 字段

---

## v1.1.0 (2026-04-12)

### ✅ 新增摸底测试功能 + 犹豫时间追踪

#### 🔍 摸底测试模式（新增）
- 覆盖全部 45 道乘法算式（1×1 到 9×9），随机顺序出题
- **每题实时计时**，显示当前思考秒数
- 进度点可视化：绿色=正确、红色=答错、黄色=犹豫（正确但耗时≥8秒）
- 答题结束后生成完整摸底报告

#### 🗺️ 摸底结果报告（新增）
- **热力图**展示：9×9 网格按颜色区分掌握程度
  - 深绿=秒答正确（≤3秒）、浅绿=正确、橙色=正确但犹豫、浅红=答错、深红=答错且犹豫
- **重点关注列表**：自动按严重程度排序（答错且犹豫 > 答错 > 犹豫）
- **完整答题记录**：支持按"只看答错"/"只看犹豫"/"全部"筛选
- **一键专练重点题**：直接跳转练习模式，范围锁定为重点题
- 支持查看上次摸底结果

#### ⏱️ 犹豫时间追踪（增强）
- 练习模式、挑战模式、摸底测试 **全部记录每题答题耗时**
- questionStats 增加 `totalTime`/`avgTime` 字段
- 默认犹豫阈值：8秒（超过则标记为犹豫题）

#### 📊 成绩单增强
- 新增 **🐢 犹豫慢题** 区域：展示平均答题时间较长的算式
- 犹豫题支持一键专练
- 犹豫题点击可跳转乘法表高亮

---

## v1.0.0 (2026-04-12)

### ✅ 初始版本发布

#### 📖 学习模式
- 完整展示 1×1 到 9×9 的乘法表
- 彩色分区显示，每一段乘法用不同颜色
- 点击算式可高亮，并语音朗读

#### ✏️ 练习模式
- 随机出题，填写答案
- 支持选择练习范围（可选1~9的乘法，或全部）
- 即时判断对错：答对有🎉动画+语音鼓励，答错有💪提示+正确答案朗读
- 跳过功能，显示正确答案
- 实时统计：已答题数、答对数、正确率
- 内置数字键盘，方便移动端使用

#### 🏆 挑战模式（限时闯关）
- 三档难度：初级(1~5, 10题)、中级(1~7, 15题)、高级(1~9, 20题)
- 计时挑战
- 进度条显示答题进度
- 完成后根据正确率获得⭐星星奖励
- 90%+获得3星，70%+获得2星，其他1星

#### 📊 学习报告
- 总体正确率环形图展示
- 总答题数、总答对数、完成闯关次数
- 薄弱项识别：正确率<70%且做过2次以上的题目自动标记
- 点击薄弱项可跳转乘法表高亮对应位置
- 一键专项练习薄弱项
- 支持清除所有学习数据

#### 🎨 UI/UX
- 大字体、大按钮，适合小朋友操作
- 响应式布局，手机/平板/电脑均可使用
- 渐变色彩设计，视觉友好
- 粒子动画和反馈动画
- 语音朗读功能（Web Speech API）
- 数据自动保存到 localStorage

---
